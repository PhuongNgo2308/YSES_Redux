const DEFAULT_STATE = {
  auth: {
    currentUser: null
  },
  loading: false,
  error: null,
  uiLoginForm: {
    email: "",
    password: "",
    isShowPassword: false
  }
};

export default DEFAULT_STATE;
